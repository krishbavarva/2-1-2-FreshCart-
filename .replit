run = "npm start"
entrypoint = "server.js"
language = "nodejs"

[nix]
channel = "stable-22_11"

[deploy]
run = ["sh", "-c", "npm run install:all && npm run build && npm start"]

[env]
NODE_ENV = "production"

